
import React from "react";
import { Button } from "@/components/ui/button";

export const BuyButton: React.FC = () => (
  <Button size="lg" className="w-full bg-primary text-white hover:scale-105 transition font-semibold text-lg shadow-md">
    Add to Cart
  </Button>
);
